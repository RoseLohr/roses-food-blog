# Podman-Compose-Definition. Start via ./deploy.sh (empfohlen) oder:
#   podman compose up -d
services:
  app:
    image: localhost/roses-blog:latest
    container_name: roses-blog
    build:
      context: .
      dockerfile: Containerfile
    env_file: .env
    environment:
      DATA_DIR: /data
      PORT: 3000
      HOSTNAME: 0.0.0.0
    ports:
      # Nur lokal erreichbar — nginx auf dem Host ist der öffentliche Zugang.
      - "127.0.0.1:${PORT:-3000}:3000"
    volumes:
      - ${DATA_DIR:-/srv/roses-blog/data}:/data
    restart: always
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://127.0.0.1:3000/health').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 15s
